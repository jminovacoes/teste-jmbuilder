import React, { useState } from 'react'; const RequestReset = ({ onSwitchToLogin }: { onSwitchToLogin?: () => void }) => { const [email, setEmail] = useState(''); const [loading, setLoading] = useState(false); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); setError(''); setMessage(''); const emailRegex = /^[^@]+@[^@]+.[^@]+$/; if (!email.trim()) { setError('Email é obrigatório'); return; } if (!emailRegex.test(email)) { setError('Email inválido'); return; } setLoading(true); setTimeout(() => { setLoading(false); setMessage('Se o e-mail estiver cadastro, você receberá um link para redefinir sua senha.'); }, 800); }; return ( <div className='auth-card'> <h2>Recuperar Senha</h2> <p className='helper-text'>Digite seu e-mail para receber um link de redefinição.</p> <form onSubmit={handleSubmit}> <div className='field'> <label htmlFor='reset-email'>Email:</label> <input type='email' id='reset-email' value={email} onChange={(e) => setEmail(e.target.value)} required /> </div> <button type='submit' disabled={loading}> {loading ? 'Enviando...' : 'Enviar Link'} </button> </form> {error && <p className='msg-error'>{error}</p>} {message && <p className='msg-success'>{message}</p>} <p className='helper-text'> <a href='#/login' onClick={(e) => { e.preventDefault(); if (onSwitchToLogin) onSwitchToLogin(); else window.location.hash = '#/login'; }}>Voltar para login</a> </p> </div> ); }; export default RequestReset;