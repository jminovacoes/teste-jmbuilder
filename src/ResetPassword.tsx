import React, { useState, useEffect } from 'react'; const ResetPassword = () => { const [token, setToken] = useState(''); const [password, setPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [loading, setLoading] = useState(false); const [message, setMessage] = useState(''); const [error, setError] = useState(''); useEffect(() => { const hash = window.location.hash; const params = new URLSearchParams(hash.split('?')[1] || ''); const t = params.get('token') || ''; setToken(t); }, []); const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); setError(''); setMessage(''); if (!token) { setError('Token inválido ou ausente.'); return; } if (password.length < 8) { setError('A senha deve ter pelo menos 8 caracteres.'); return; } if (password !== confirmPassword) { setError('As senhas não coincidem.'); return; } setLoading(true); setTimeout(() => { setLoading(false); setMessage('Senha redefinida com sucesso!'); }, 800); }; return ( <div className='auth-card'> <h2>Redefinir Senha</h2> <p className='helper-text'>Digite uma nova senha (mínimo 8 caracteres).</p> <form onSubmit={handleSubmit}> <div className='field'> <label htmlFor='new-password'>Nova Senha:</label> <input type='password' id='new-password' value={password} onChange={(e) => setPassword(e.target.value)} required /> </div> <div className='field'> <label htmlFor='confirm-password'>Confirmar Senha:</label> <input type='password' id='confirm-password' value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} required /> </div> <button type='submit' disabled={loading}> {loading ? 'Salvando...' : 'Redefinir Senha'} </button> </form> {error && <p className='msg-error'>{error}</p>} {message && <p className='msg-success'>{message}</p>} </div> ); }; export default ResetPassword;